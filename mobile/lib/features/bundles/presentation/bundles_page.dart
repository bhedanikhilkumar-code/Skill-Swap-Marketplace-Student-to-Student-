import 'package:flutter/material.dart';

class BundlesPage extends StatelessWidget {
  const BundlesPage({super.key});

  @override
  Widget build(BuildContext context) => const Scaffold(body: Center(child: Text('Skill Bundles')));
}
