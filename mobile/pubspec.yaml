name: skill_swap_mobile
description: Skill Swap Marketplace mobile app
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.4.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  go_router: ^14.2.0
  dio: ^5.5.0+1
  flutter_secure_storage: ^9.2.2
  socket_io_client: ^3.0.0
  firebase_core: ^3.3.0
  firebase_messaging: ^15.0.4
  flutter_dotenv: ^5.1.0
  shared_preferences: ^2.3.2
  connectivity_plus: ^6.0.5
  flutter_webrtc: ^0.11.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
